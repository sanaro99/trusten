# Copy this file to .env.development for local development

# Set to true to use BrowserOS binary, false to use default Chrome
USE_BROWSEROS_BINARY=true

# Agent server port (used by extension, optional - reads from BrowserOS prefs if not set)
VITE_BROWSEROS_AGENT_PORT=9200

# Posthog Analytics
VITE_PUBLIC_POSTHOG_KEY=
VITE_PUBLIC_POSTHOG_HOST=

# Sentry DSN
VITE_PUBLIC_SENTRY_DSN=

# Sentry project config
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# BrowserOS launch args (used by web-ext.config.ts)
# See BrowserOS-server/.env.example, set the same ports there so browseros-server's bun start will automatically connect to this browseros with web-ext

# Disables running browseros-server packaged in BrowserOS so we can run the dev server
BROWSEROS_DISABLE_SERVER=true

# CDP port where the browseros-server connects when `bun start`
BROWSEROS_CDP_PORT=9000

# MCP port where the browseros-server listens for MCP requests
BROWSEROS_MCP_PORT=9100

# Agent server port where the browseros-server listens for agent requests
BROWSEROS_AGENT_PORT=9200

# BrowserOS agent controller websocket port
BROWSEROS_EXTENSION_PORT=9300

# User data directory (profile location) to avoid polluting your main browser profile
BROWSEROS_USER_DATA_DIR=/tmp/browseros-dev
